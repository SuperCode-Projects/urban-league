import React, { Component } from 'react';
import { firestore } from "../firebase";

class Map extends Component {
    constructor(props) {
        super(props);
        this.state = {}
    }
    render() {
        return (null);
    }
}

export default Map;